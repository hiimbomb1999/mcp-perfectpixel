## What does this PR do?

<!-- Briefly describe the change and why it's needed. -->

## Checklist

- [ ] Tests pass — `pnpm test` (builds both packages, then runs the full suite)
- [ ] Lint is clean — `pnpm lint` (eslint + prettier --check)
- [ ] README tool reference updated if the MCP schema changed
- [ ] Scope guardrails respected — no per-framework parsers, no patch application
